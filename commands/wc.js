exports.run = (Discord,rdc,client,message,cmd) =>{
    if(message.author.id!==process.env.BOSS_ID && message.member.permissions.FLAGS!=="ADMINISTRATOR") return;
    rdc.get("welcome"+guild.id,function(err,reply){
        
    });
}